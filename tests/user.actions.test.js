import { use, expect, assert, should } from 'chai'

describe('User Tests', () => {
    it('User should be created upon valid registration', () => {

    })
    it('User registration should fail for empty username', () => {

    })
    it('Registration should fail for existing user', () => {

    })

    it('Registration should succeed for non-existing user with valid password', () => {

    })

    it('Log in should fail for existing user and wrong password', () => {

    })

    it('Log in should fail for empty username field', () => {

    })

    it('Log in should fail for empty password field', () => {

    })

    it('Log in should fail for invalid username', () => {

    })

    it('Log in should fail for invalid password', () => {

    })
   
   
    
})