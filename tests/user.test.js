import { use, expect,assert, should } from 'chai'
import request from 'supertest';

describe('User validation tests', () => {
    it('Registration should fail for empty username field', () => {

    })
    it('Registration should fail for empty password field', () => {

    })
    it('Registration should fail for invalid password entered', () => {

    })


    
})